# 竞拍之王计算器

React + Electron 桌面计算器，用于根据颜色品质、均格、可能件数和总藏品数推算竞拍估值。

## 开发运行

```powershell
npm install
npm run dev
```

开发地址默认是 `http://localhost:5174`。

## 构建前端

```powershell
npm run build
```

## 打包桌面版

```powershell
npm run pack:desktop
```

生成结果在 `release-latest/win-unpacked`。
